# 소울타입 / Soultype — 인수인계

작성: 2026-08-27 (상품성 개정 + 토스 해요체). 이전 심층본은 아래 핵심만 남기고 **구속력을 낮춘다.**

저장소: https://github.com/Remmikim-bit/soultype (`main`)

---

## 상품성 개정 — 다음 에이전트는 이 절이 우선

이전 HANDOVER는 실패 실험을 잠그는 문서였다. 그게 레이아웃·테마·동선 개선을 막았다. **제품이 먼저다.** 아래는 풀어 쓴 것과, 진짜로 남기는 것.

### 풀어도 되는 것 (이번 턴에서 품)

| 옛 구속 | 왜 풀었나 | 지금 |
|---|---|---|
| 다크만 | 사용자가 라이트/다크를 명시. 모바일 락인은 라이트가 기본인 앱을 이긴다 | `data-theme` + `st-theme` 로컬 저장. 시스템 선호 따름 |
| 오버레이 40rem 왼쪽 고정, 프로시니엄 22dvh | 16:9에서 글자가 커지거나 오른쪽이 빈다 | 컬럼 `min(26.5rem, 36vw)`, 와이드에서 유체가 **오른쪽 무대**를 채움 |
| 블롭 위치/카메라 수치 고정 | 유체가 좌하단 잔상에 붙음 | 라바램프: 상하 + 전후(z→크기). 레이아웃 오프셋 |
| 카피 문자열 신성불가침 | ux-flow 회귀용이지 제품 카피가 아님 | **QA가 잡는 핵심 구만** 유지. 나머지 문장은 토스 해요체 |
| 글래스 86% 고정 | 라이트 모드에서 진흙이 됨 | 다크는 글래스, 라이트는 토스형 솔리드 카드 |
| 제목 400 / text-7xl | 16:9에서 화면을 먹음 | 산세리프 600, `clamp` 히어로 |
| “추측으로 동선 넣지 마라” | 사용자가 락인·광고 노출 동선을 요청 | 진행 2/6, 이어서 카드, 결과 다음 시험, 네이티브 광고 슬롯 |

### 진짜로 남기는 것 (깨면 제품이 죽음)

1. **원문 대화 서버 금지.** `UsageDigest`만. `XAI_API_KEY`는 서버 전용.
2. **인증 끄기.** `VITE_AUTH_ENABLED=false`. 계정/DB 켜지 마라.
3. **이산 도형(구/박스/도넛)으로 되돌리지 마라.** 파라미터 필드 + 메타볼 `smin`.
4. 바이럴 문장: `지금 쓰는 AI의 MBTI는 {TYPE}이에요.` — 사용자 본인 유형이 아님. 해요체.
5. 한국어 UI. 크롬에 이모지 없음. 16 캐릭터 이름은 퀴즈 라벨.
6. Grok 필/Remix, `PreviewHostBridge`, `startup.sh`, `npm run dev`. `og`를 `x:game`으로 두지 마라.
7. 커밋 이름은 **Remmikim-bit**.
8. 광고 닫기 ≠ 언락. 타이머 끝나기 전 `결과 열기` 금지.

### 토스 UX 라이팅 (소비자 가이드를 제품 카피에 적용)

- **해요체** 전 문구. 반말·합니다체 없음.
- **능동** (`했어요`), **긍정** (길이 안 되면 할 수 있는 길을 제시). 진단 결과는 부정 예외 가능.
- **캐주얼 경어.** `시겠어요` / `께` 금지. `드려요` `볼게요`는 됨.
- **Easy to speak.** 소리 내어 읽었을 때 친구에게 말해도 자연스럽게. 전보체·짧은 딱딱한 종결 금지.
- **CTA는 다음 화면.** `분석 시작하기`, `광고 보고 결과 보기`, `결과 열기`. 다이얼로그 왼쪽은 **닫기**.
- `{명사}+{명사}`는 동사로 풀기. 시험 이름(`AI의 스트레스 지수`)은 QA 라벨이라 유지.

### 토스에서 가져온 UX (브랜드 카피는 민트, 블루 아님)

- 한 화면 한 액션. 큰 숫자, 말하듯 한국어, 풀폭 프라이머리.
- 홈 = 진행 + 다음 카드 + 목록. 결과 = 잠금 해제 보상 + **이어서**.
- 광고는 인터럽트가 아니라 동선의 칸: 허브 네이티브 → 극장 대기 → 리워드 게이트 → 결과 인라인 → 다음 시험.
- 터치 44px+, 390 폭 가로 스크롤 없음. 16:9는 폰 컬럼 + 유체 무대.

### 파일 (이번 개정)

```
src/lib/theme.ts                  라이트/다크
src/components/theme-toggle.tsx
src/components/continue-strip.tsx 결과 → 다음 시험
src/styles.css                    토큰, 16:9 컬럼, 라이트 솔리드
src/lib/soul-shader.ts            라바램프 궤도 + z 스케일
src/components/soul-field.tsx     와이드면 오른쪽, 모바일이면 위
src/routes/index.tsx              허브 진행/이어서
src/lib/scores.ts                 결과 해요체
src/lib/characters.ts             16유형 해요체
```

ux-flow가 잡는 구: `내 AI 성격 분석`, 시험 이름, `이 질문을 넣고`, `분석 시작하기`, `광고 보고 결과 보기`, `한낮노트`/`늦은우체국`, `지금 쓰는 AI의 MBTI는`, `이미지 프롬프트 3개`, `대화록 있음`, `기록 지우기`, `대화록 여기`, `한눈에 보는 내 사용 버릇`, `간단하게 알아보기`. 바꾸면 `scripts/ux-flow.mjs`를 같이.

---

## 한 줄

**지금 쓰는 AI의 MBTI를 읽는, 프라이버시 우선 한국어 웹앱.** 무대는 3D 유체, 글자는 토스형 카드, 결과는 광고 뒤, 다음 시험이 락인이다.

---

## 주인 / 환경

| 항목 | 값 |
|---|---|
| GitHub | **Remmikim-bit** / `Remmikim-bit/soultype` |
| UI | 한국어만, 해요체 |
| 인증 | OFF |
| 미리보기 | `0.0.0.0:8080`, `startup.sh` + `npm run dev` |

원문 파서·4축·st.v1·광고 타이머·캐릭터 lock은 코드가 진실이다. `src/lib/relay.ts`, `axes.ts`, `analyze.ts`, `parse-export.ts`, `characters.ts`를 읽으면 된다.

실패 실험 (되풀이 금지, 요약):

- 5개 SDF를 동시에 섞기 → 라벨만 바뀜
- 토러스 이산 도형 / 가중치 보간으로 구멍 유지 → 구멍이 죽음
- 카메라 2.28, 스케일 1.14 → 타이포를 먹음
- 13g 캔디 필 + 3D
- 블롭 4개 × 스텝 높게 → Playwright 타임아웃

n8n, 계정, 캐릭터 초상, Gemini, 이산 도형 갤러리는 사용자가 거절했다. 실제 광고 SDK는 아직 요청 없음(시뮬 유지).
